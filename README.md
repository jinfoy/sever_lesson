# サーバレッスン
Linuxプラクティス
___________________

<p>$ ls /</p>
<p>$ ls / usr </p>
$ cd 
$ pws
$ mkdir(make directory)
$ touch
$ &&
$ cp(copy)
$ cp -r
$ mv(move)
$ rm(remove)
$ rm -r
$ rm -rf /
i$ *
$ ?
$ >
$ >>
$ cat(catnate)
$ vi
  
