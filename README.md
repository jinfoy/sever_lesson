# サ�サーバ�レッスン
Limuxプラクティス�
___________________

$ ls /
$ ls / usr
$ cd 
$ pws
$ mkdir(make directory)
$ touch
$ &&
$ cp(copy)
$ cp -r
$ mv(move)
$ rm(remove)
$ rm -r
$ rm -rf /
$ *
$ ?
$ >
$ >>
$ cat(catnate)
$ vi
  
