# サーバレッスン
Linuxプラクティス
___________________

$ ls / \r
$ ls / usr \r
$ cd 
$ pws
$ mkdir(make directory)
$ touch
$ &&
$ cp(copy)
$ cp -r
$ mv(move)
$ rm(remove)
$ rm -r
$ rm -rf /
i$ *
$ ?
$ >
$ >>
$ cat(catnate)
$ vi
  
